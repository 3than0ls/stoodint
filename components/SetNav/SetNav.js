import React, { useEffect, useState } from 'react'
import axios from 'axios'

export default function QuestionsNav({ questionSets }) {
  return <div className="w-full bg-red-300"></div>
}

// use getStaticProps when moving this to a /pages and passing the props down to this component
