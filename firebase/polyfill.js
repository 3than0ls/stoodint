// instead of writing al this garbage
// import XHMLHttpRequest from 'xhr2'
// global.XMLHttpRequest = XHMLHttpRequest
// import { decode, encode } from 'base-64'
// global.btoa = encode
// global.atob = decode

// we can write this and firebase web still works
import 'core-js'
